namespace auernautica_imperiali {
    public class AOrk : AUnit {
        public AOrk(int x, int y, int z, int structure, int speed, int throttle, int minSpeed, int maxSpeed, int maneuver, int handling, int maxAltitude) : base(x, y, z, structure, speed, throttle, minSpeed, maxSpeed, maneuver, handling, maxAltitude, 1) {
        }
    }
}